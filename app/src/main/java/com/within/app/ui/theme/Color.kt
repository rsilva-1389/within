package com.within.app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette — core
val Warm = Color(0xFFFE856C)        // coral — CTAs, accent
val Supportive = Color(0xFFFEB97E)  // peach — secondary accent
val Gentle = Color(0xFFFEE2A3)      // cream — inactive dots, subtle surfaces
val Calm = Color(0xFFBFD9CC)        // sage — surfaces
val Positive = Color(0xFF3D8186)    // teal — final CTA, secondary actions
val Encouraging = Color(0xFF001B39) // navy — headings, dark text

// Light onboarding palette
val OnboardingBg = Color(0xFFFBF3E7)         // warm cream background
val OnboardingBgSage = Color(0xFFF1F4EF)     // sage background (reminder step)
val OnboardingText = Color(0xFF001B39)        // dark navy text
val OnboardingTextSecondary = Color(0xFF5A6B6E) // muted teal-gray
val OnboardingDot = Color(0xFFE8DDCB)         // inactive progress dot
val OnboardingCard = Color(0xFFF0E6D4)        // card border

// Dark theme semantic aliases (home, settings)
val WithinPrimary = Positive
val WithinSecondary = Calm
val WithinBackground = Encouraging
val WithinSurface = Color(0xFF002a55)
val WithinOnPrimary = Gentle
val WithinOnBackground = Gentle
val WithinOnSurface = Color(0xFFE8F4F0)
val WithinAccent = Warm
